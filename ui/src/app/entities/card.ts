  export enum Properties {
    Id = "id",
    Uid = "uid",
    Holder = "holder",
    Expiry = "expiry",
    Validity = "validity",
  }

  export enum CardLocation {
    Lost = "lost",
    Stolen = "stolen",
  }
  export enum CardStatus {
    Active = "activeMember",
    Revoked = "revoked",
    NonMember = "nonMember",
    Inactive = "inactive",
    Expired = "expired",
  }
