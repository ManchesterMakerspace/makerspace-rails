<% type = @invoice.resource_class %>
<% isMembership = type == "member" %>
<% friendly_type = isMembership ? 'membership' : 'rental' %>

<div style="width: 100%">
  <h3> Disputed <%= friendly_type %> payment to Manchester Makerspace</h3>
  <div>
    <p>
      Our payment processor has reviewed the dispute for <%= @invoice.name %> and have declined to accept the dispute. No refund will be issued.
    </p>
  </div>
</div>