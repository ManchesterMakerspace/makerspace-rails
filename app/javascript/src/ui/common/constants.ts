import { TextField, Radio, Checkbox } from "@material-ui/core";

export const FormTypes = new Set([
  TextField,
  HTMLInputElement,
  HTMLSelectElement,
  HTMLTextAreaElement,
  Radio,
  Checkbox,
]);